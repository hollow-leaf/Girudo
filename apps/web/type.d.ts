export type RpcNode = {
  name: string;
  url: string;
  latency: number;
};

export type UserInfo = {
  username: string;
  avater: string;
  email: string;
  suiAddress: string;
}
export type GUILD = "PSYDUCK";
